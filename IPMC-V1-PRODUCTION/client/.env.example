VITE_API_URL=http://localhost:5000/api
# URL of the separately-deployed admin app (used only for the footer's
# "Admin" link — the admin dashboard is its own Vite app, not a route
# inside this one). Leave unset in dev; falls back to /admin.
VITE_ADMIN_URL=http://localhost:5174
